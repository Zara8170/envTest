name: Build and Push Docker Images
 
on:
  push:
    branches:
      - backend
jobs:
  build_and_push: 
    runs-on: ubuntu-latest

    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build MySQL Docker image
        working-directory: database
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/database .

      - name: Push MySQL Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/database

      - name: Build Backend Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/backend .
      - name: Push Backend Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/backend

  deploy:
    needs: build_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Docker pull and run
        uses: appleboy/ssh-action@master
        with:
          username: ubuntu
          host: ${{ secrets.EC2_HOST_IP }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            if [ "$(sudo docker ps -a -q -f name=database)" ]; then
              sudo docker rm -f dockerdb
            fi 
            if [ "$(sudo docker ps -a -q -f name=backend)" ]; then
              sudo docker rm -f dockerbackend
            fi
            if [ "$(sudo docker images -q ${{ secrets.DOCKERHUB_USERNAME }}/database)" ]; then
              sudo docker rmi -f ${{ secrets.DOCKERHUB_USERNAME }}/database
            fi
            if [ "$(sudo docker images -q ${{ secrets.DOCKERHUB_USERNAME }}/backend)" ]; then
              sudo docker rmi -f ${{ secrets.DOCKERHUB_USERNAME }}/backend
            fi
            sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/database
            sudo docker run -d -p 3308:3306 --name dockerdb -v db_volume:/var/lib/mysql ${{ secrets.DOCKERHUB_USERNAME }}/database
            sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/backend
            sudo docker run -d -p 8080:8080 --name dockerbackend --add-host=host.docker.internal:172.17.0.1 ${{ secrets.DOCKERHUB_USERNAME }}/backend
